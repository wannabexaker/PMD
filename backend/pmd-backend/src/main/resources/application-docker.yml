server:
  port: 8080

spring:
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://mongo:27017/pmd}
pmd:
  jwt:
    secret: ${PMD_JWT_SECRET}
  security:
    rate-limit:
      per-minute: ${PMD_RATE_LIMIT_PER_MINUTE:600}
      per-hour: ${PMD_RATE_LIMIT_PER_HOUR:12000}
